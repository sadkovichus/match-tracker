import { MatchList } from '@features/matches-list'
import { ContainerStyle } from './styles'

export const HomePage = () => {
  return (
    <ContainerStyle>
			<MatchList />
    </ContainerStyle>
  );
};
